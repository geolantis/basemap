# Local Development Environment Configuration
# Copy this file to .env.local and fill in your values

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# For admin operations (like adding users)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Environment (development/production)
NODE_ENV=development

# Local Development URL (default for Vite)
VITE_APP_URL=http://localhost:5173

# Optional: Custom port if not using default
# VITE_PORT=5173

# API Configuration for local development
VITE_API_BASE_URL=http://localhost:5173

# Map Provider API Keys (optional for local testing)
# These are usually handled by the proxy, but you can add them for direct testing
# MAPTILER_API_KEY=your_maptiler_key
# CLOCKWORK_API_KEY=your_clockwork_key
# BEV_API_KEY=your_bev_key